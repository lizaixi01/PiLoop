# 0001 — PiLoop 原生 Pi 执行路径

状态：已修正。日期：2026-09-11。

此前实现将全部输入截获后交给静态网页 Runner，导致问候也创建任务，并使原生 TUI 沦为另一套执行器的外壳。该路径已从默认 CLI 移除。

现在普通输入直接进入 Pi 会话，恢复默认工具、项目上下文加载、输入编辑器和底部状态栏。没有 input/user_bash 拦截、没有自定义 header/footer，也不关闭默认工具。SessionManager 与运行服务使用同一个用户工作目录。启动不再创建 Express 服务、不选择旧网页项目。

PiLoop 只注入 /api 快捷配置和可选项目约束扩展。用户显式保存到 .pi/piloop-memory.json 的范围约束在 before_agent_start 中附加有限上下文，不驱动工具执行。当前是模型解释范围的简单机制，不能宣称已经实现可靠自动召回。

发布版账号、模型配置及会话保存在 ~/.piloop/agent；旧安装目录配置仅在目标不存在时复制迁移。默认 CLI 的代码修改现在直接作用于真实工作目录，与 Pi 一致；旧网页原型仍独立保留副本流程。原型反馈没有自动迁移，避免旧示例约束影响真实项目。

全局命令依赖编译后的 lib，必须在真实安装环境完成构建并验证，不能只验证开发环境的源码或隔离编译产物。

模型上下文追加简短的 PiLoop 能力说明，明确原生 Pi 与扩展的归属、显式约束的范围解释限制和未实现功能。该说明不触发工作，也不作为产品差异已经成立的证据。协议测试核实说明进入真实 SDK 请求。

主动记忆采用一个 remember_preference 工具，复用当前模型判断，不引入第二模型、后台抽取或新 UI。input 钩子只记录原始输入后 continue，不接管 Pi 对话。保存须有当前用户逐字引用与持续意图线索，敏感行为需确认。判断规则保守但不完备；不能宣称所有纠正都会被可靠提炼。

记忆问句修正：不再将问句语法等同于没有持续意图。“你会自动记住我需要简单解释的需求吗”包含记忆请求和明确偏好，允许直接保存到 user 层；纯能力提问、一次性要求、否定保存与假设示例仍拒绝。保留原文、范围和 /forget，且不增加额外模型调用。21 项测试及构建通过；真实 Pi SDK 的脚本化供应商测试覆盖此原句的保存、跨项目新会话注入。该测试证明工具与持久化通路，不证明真实模型必然调用工具；判断仍是保守启发式，并非完整语义解析。

后续真实失败显示“你又忘了，请解释的时候简单点”已触发模型保存调用，但被持续意图词表拒绝。现在完整当前输入中的重复纠正线索可支持交流偏好片段的持久化；仅保存逐字引用的偏好，不保存同一句中的技术问题。默认解释约定要求先回答具体疑惑，避免顺带知识展开和不必要的项目检索。原生 SDK 回归改为覆盖混合纠正/问题的片段保存和跨项目新会话注入；保留记忆问句、否定、临时请求的独立测试。仍属于保守启发式，不能宣称解决了任意偏好抽取。

一般交流偏好存 agent/preferences.json，跨项目恢复；项目规则仍在项目 .pi/piloop-memory.json。仅语言、回复长度和解释风格允许自动使用 user 层。两层共用 /memory 和 /forget、同一注入预算；具体项目要求优先于一般偏好。未迁移任何旧项目规则到全局。

主动交互第一轮实验只增加一段系统上下文：先按需进行只读调查，明确任务直接完成，实质意图分歧才给选项，选择后继续执行。保留正常聊天和权限边界，不强制三选一，不新增 UI 或后台执行器。行为收益尚须真实轨迹验证，实验协议见 ../experiments/proactive-pilot.md。

DeepSeek-Flash 的 16 会话初步对照未显示额外收益：明确任务和 A 选择后的执行两组都成功；模糊任务表现不稳定，两组各有一次添加无对应页面的链接。实验缺少目录查找工具，限制了对主动获取上下文的判断。暂不扩大机制；后续实验先修正工具条件与分歧样本。详见实验记录。

后续补齐实验查找工具后仍未证明新增主动规则的稳定收益。针对捏造链接加入最小范围约定：呈现改进不顺带新增路由/内容，明确导航请求则检查或创建目的地。12 会话范围回归中，两组都完成明确排版与新页面任务，模糊任务两组均停在澄清阶段，故没有额外收益结论。产品保留此行为约定但不声称强制执行；后续需验证完整决策与执行闭环，见 ../experiments/scope-pilot.md。

发布版 0.1.2 收敛为 Pi 原生 CLI + 主动执行约定 + 有范围的偏好记忆。旧网页原型只作为源码开发实验；React、Express、Playwright 转为 devDependencies，发布文件白名单仅包含启动器与五个 CLI 模块。范围解析独立于旧网页 Store，防止 CLI 间接加载原型逻辑。Node 最低版本对齐当前 Pi SDK 的 22.19。发布前以临时独立 prefix 安装实际 tarball，检查依赖与运行时启动；保留原生更新通知和手动 update。

网页验收增量：默认执行约定增加实现→运行→修复→最终版本复查，作为 Harness 行为而非用户记忆。新增第六个 CLI 模块 web-check，仅在调用 verify_web_page 时解析可选浏览器驱动，不恢复旧网页服务或自定义 TUI。优先复用项目驱动，其次 ~/.piloop/browser；Windows 无 Chromium 时复用 Edge。工具三次调用预算和真实结果报告不等于强制交付门禁。保留不完整验证的明确说明，不声称零缺陷。真实 DeepSeek-Flash 单例成功闭环及局限见 ../experiments/web-verification-pilot.md。

启动优化（2026-09-11）：公开 SDK 入口会加载分散模块，首次观察单独导入 13.155s，PiLoop 扩展导入 0.333s。Pi 0.85.1 已附带 dist/bundle/index.js，复用该构建产物而非自行重打包；pi-sdk 适配层检查版本及必需导出，失败回退公开入口，支持 PILOOP_UNBUNDLED_SDK=1。该 bundle 路径非公开 exports，升级依赖必须重新验证；保留原生资产位置、登录、TUI、工具与会话行为。

交替启动三次/路径，独立进程及空配置，运行时就绪：公开入口 1709/2123/1792ms；打包入口 666/632/581ms，中位数减少约 65%。未清理 OS 缓存，不能称为冷启动或终端首帧数据；最初 13s 仅为单次慢样本。22 项测试、类型检查/构建、干净安装/启动通过。真实用户配置的原生 TUI、/model 菜单、退出已验证。基准脚本 scripts/startup-bench.mjs 可重跑。

v0.1.4 发布前的 12 次真实会话消融实验未证明新约定提升首次交付成功：对照 6/6、新约定 5/6（一次错误测试导致超时）；最终文件独立验收两组均 6/6。记录全部结果，禁止把单次成功或最终文件通过当成整体首次交付收益。详见 ../experiments/web-delivery-eval-014.md。启动只增加一行版本通知，更新检查成功缓存一小时，离线失败不缓存，保持原生输入和底栏。
